# /ptt-status

Check the PTT daemon status.

Use the `mcp__ptt__ptt_get_status` tool to get the current status including:
- Whether the daemon is running
- Recording/transcribing state
- Last error (if any)
- Configuration status
